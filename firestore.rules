rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /chats/{senderId}/{receiverId}/{messageId} {
      allow create: if request.auth != null && request.auth.token.email == senderId;
    }
  }
}